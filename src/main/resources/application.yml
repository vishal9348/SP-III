server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:3307/workflow
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin

kafka:
  bootstrap-servers: kafka:9092